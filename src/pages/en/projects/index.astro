---
import ProjectsIndexScreen from "@/screens/ProjectsIndexScreen.astro";
import { getProjectCaseStudies } from "@/data/project-case-studies";

const locale = "en";
const projects = getProjectCaseStudies(locale);
---

<ProjectsIndexScreen locale={locale} projects={projects} />
