---
import BaseLayout from "@/layouts/BaseLayout.astro";
import PageHero from "@/components/PageHero.astro";
import { Award, ExternalLink, FileText } from "lucide-astro";
import { getContent, type Locale } from "@/i18n";

type Props = { locale: Locale };
const { locale } = Astro.props;
const content = getContent(locale);
const page = content.pages.awards;
---

<BaseLayout locale={locale} title={page.title} description={page.description}>
  <PageHero eyebrow={page.title} title={page.heroTitle} description={page.heroDescription} />
  <section class="container-pad py-14">
    <div>
      <h2 class="text-2xl font-bold">{page.awardsTitle}</h2>
      <div class="mt-5 grid gap-5 md:grid-cols-2">
        {content.awards.map((award) => (
          <article class="surface overflow-hidden rounded-lg">
            {award.image ? (
              <a href={award.image} target="_blank" rel="noreferrer" class="group relative block" aria-label={`${content.ui.openDocument}: ${award.title}`}>
                <img src={award.image} alt={award.title} class="aspect-[4/3] w-full bg-slate-100 object-cover object-top dark:bg-slate-800" loading="lazy" />
                <span class="absolute inset-x-4 bottom-4 inline-flex items-center justify-center gap-2 rounded-md bg-white/95 px-4 py-2 text-sm font-bold text-river shadow-sm ring-1 ring-slate-200 transition group-hover:-translate-y-0.5 group-hover:bg-river group-hover:text-white dark:bg-slate-950/90 dark:text-blue-200 dark:ring-slate-700 dark:group-hover:bg-river dark:group-hover:text-white">
                  {content.ui.viewDocument}
                  <ExternalLink class="h-4 w-4" aria-hidden="true" />
                </span>
              </a>
            ) : (
              <div class="grid aspect-[4/3] place-items-center bg-slate-100 text-teal dark:bg-slate-800 dark:text-teal-300">
                <Award class="h-12 w-12" aria-hidden="true" />
              </div>
            )}
            <div class="p-5">
              <h3 class="text-xl font-bold">{award.title}</h3>
              <p class="mt-2 text-sm leading-7 text-slate-600 dark:text-slate-300">{award.organization}</p>
              <p class="mt-1 text-sm font-semibold text-copper dark:text-amber-300">{award.year}</p>
              <p class="mt-3 text-sm leading-7 text-slate-700 dark:text-slate-200">{award.description}</p>
              <p class="mt-3 text-xs font-bold text-slate-500 dark:text-slate-400">{content.ui.project}: {award.project}</p>
              {award.image && (
                <a href={award.image} target="_blank" rel="noreferrer" class="mt-4 inline-flex items-center gap-2 text-sm font-bold text-river dark:text-blue-300">
                  {content.ui.openDocument}
                  <ExternalLink class="h-4 w-4" aria-hidden="true" />
                </a>
              )}
            </div>
          </article>
        ))}
      </div>
    </div>
    <div class="mt-14">
      <h2 class="text-2xl font-bold">{page.recommendationsTitle}</h2>
      <div class="mt-5 grid gap-4 md:grid-cols-2">
        {content.recommendations.map((rec) => (
          <article class="surface rounded-lg p-5">
            <FileText class="h-6 w-6 text-teal dark:text-teal-300" aria-hidden="true" />
            <h3 class="mt-3 text-xl font-bold">{rec.title}</h3>
            <p class="mt-2 text-sm text-slate-600 dark:text-slate-300">{rec.organization} - {rec.author}</p>
            <p class="mt-1 text-sm font-semibold text-teal dark:text-teal-300">{rec.date}</p>
            <p class="mt-3 text-sm leading-7 text-slate-700 dark:text-slate-200">{rec.evidence}</p>
            {rec.document && (
              <a href={rec.document} target="_blank" rel="noreferrer" class="mt-4 inline-flex items-center gap-2 rounded-md border border-river/30 px-3 py-2 text-sm font-bold text-river transition hover:bg-river hover:text-white dark:border-blue-300/30 dark:text-blue-300">
                {content.ui.viewDocument}
                <ExternalLink class="h-4 w-4" aria-hidden="true" />
              </a>
            )}
          </article>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>
