import { enContactLabels } from "./contact";
import { enNavigation } from "./navigation";
import { enPages } from "./pages";
import { enUi } from "./ui";

export const enLocaleContent = {
  ui: enUi,
  navigation: enNavigation,
  pages: enPages,
  contactLabels: enContactLabels
};
