export type ExpertiseItem = {
  title: string;
  summary: string;
  proof: string;
  items: string[];
};

export type RelatedExperienceItem = {
  title: string;
  summary: string;
  tags: string[];
};

export type GroupItem = {
  title: string;
  items: string[];
};
